import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Nike-Application-Design',
  webDir: 'dist'
};

export default config;
